//this lesson tells us Just like normal functions we can pass arguments to arrow functions.
const myConcat = (arr1, arr2) => arr1.concat(arr2);
// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
