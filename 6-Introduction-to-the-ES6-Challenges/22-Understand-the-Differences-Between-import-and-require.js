//This lesson tells about Import and Export. We can export a code in one file and import in another file. It allows only certain variables.
import { capitalizeString } from "string_functions"
const cap = capitalizeString("hello!");
