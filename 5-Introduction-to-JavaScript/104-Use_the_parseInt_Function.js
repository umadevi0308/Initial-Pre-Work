//This lesson is about parseInt function. It takes a string and returns in integer,if the string can't be converted into a number, then it returns NaN.
function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
