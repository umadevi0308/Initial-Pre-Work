//This lesson is about the Ternary Operator.The conditional operator, also called the ternary operator, can be used as a one line if-else expression.
function checkEqual(a, b) {
  return a === b ? true : false;
}

checkEqual(1, 2);
