// This lesson shows how the reminder operator looks like and it looks % in JS. It also determines the reminder number is evern or odd.
// Only change code below this line

var remainder;
remainder = 11 % 3;
